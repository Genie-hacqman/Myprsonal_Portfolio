// Contact page content and contact form component.

import Contact from '../components/Contact';

// ContactPage acts as a route wrapper around the shared contact form section.
const ContactPage = () => {
  return <Contact />;
};

export default ContactPage;
